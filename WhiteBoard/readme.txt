画笔刷、各种图形、橡皮、文字、清屏、调节笔刷粗细、颜色
显示当前在线成员
上线后可以获取历史绘图记录并显示
实时同步共享绘图内容

主窗口关闭后白板窗口也会一并关闭

服务器掉线后客户端自动关闭
客户端掉线后服务器自动广播更新成员列表

存在问题：
应用层协议没有确认、重传机制，客户端初始化时默认收到的第一个数据是自己的ID，如果实际上收到的是其他用户的绘图信息，就会产生问题。
最多同时连接5个客户端（可根据需求和性能调整）

使用request，response两种头方便将来实现确认、重发机制


学到的知识：
socket建立tcp连接
socket接收、发送的细节，会阻塞进程，通过socket进行数据的编码发送、接收解码
socket相关的错误处理
设计协议，设计头字段结构和语义
GUI设计，实现画板
多线程
